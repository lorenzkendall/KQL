// References table + total # events
AuthenticationEvents
| count 

// Pulls 5 data samples
AuthenticationEvents
| take 5

// Same + specifies 3 columns
AuthenticationEvents
| take 5
| project username, timestamp, result

